relate [R] is deploying to {{ .Release.Namespace }}.

  Web:  {{ if .Values.ingress.enabled }}https://{{ .Values.ingress.host }}{{ else }}kubectl port-forward svc/{{ include "relate.fullname" . }}-web 3000:3000{{ end }}
  Build the web image with NEXT_PUBLIC_API_URL={{ .Values.publicApiUrl }}.

{{- if .Values.ollama.enabled }}
  Load the model once (air-gapped: copy the model blobs into the PVC instead):
    kubectl exec -n {{ .Release.Namespace }} statefulset/{{ include "relate.fullname" . }}-ollama -- ollama pull {{ .Values.config.OLLAMA_MODEL }}
{{- end }}
{{- if .Values.networkPolicy.enabled }}
  NetworkPolicy "zero-egress" is active: pods cannot reach the internet.
{{- end }}
